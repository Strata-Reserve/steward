{
  "name": "@stwd/webhooks",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "bun test"
  },
  "dependencies": {
    "@stwd/shared": "workspace:*",
    "@stwd/db": "workspace:*",
    "drizzle-orm": "^0.44.5"
  },
  "description": "Webhook dispatch and retry queue for Steward transaction events",
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
