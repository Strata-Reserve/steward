{
  "name": "@stwd/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "bunx @biomejs/biome check src package.json tsconfig.json",
    "test": "bun test src/__tests__/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^25.5.0",
    "typescript": "^5.8.0"
  },
  "description": "Financial-service adapter interfaces and mock/provider implementations for Steward (swaps, earn, onramp, offramp, KYC, TOS, custodial wallets, push) — real providers pluggable via the registry"
}
