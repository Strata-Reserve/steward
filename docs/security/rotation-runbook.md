# Secret rotation runbook

The canonical, code-verified operator procedure is [`../runbooks/key-rotation.md`](../runbooks/key-rotation.md).

This compatibility path is retained for existing links. Do not follow older copies of this page that describe `STEWARD_JWT_SECRET_NEXT`; Steward does not consume that variable and currently has no symmetric JWT overlap keyring.
